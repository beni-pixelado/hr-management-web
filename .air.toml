root = "."
tmp_dir = "tmp"

[build]
cmd = "sh -c 'pid=$(lsof -t -i:8000); if [ -n \"$pid\" ]; then echo \"Killing process on port 8000: $pid\"; kill -9 $pid || true; fi; go run ./backend/cmd/server'"
include_ext = ["go", "html", "tmpl", "tpl"]
exclude_dir = ["tmp", "vendor"]